[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "merps-source-analysis"
version = "1.0.0"
description = "Source-explicit EEG-fNIRS emotion regression analysis"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
